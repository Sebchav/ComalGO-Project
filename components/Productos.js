import React from "react";
import { Text } from "react-native";
import Producto from "../components/Producto";

const Productos = () => {
  return <Producto nombreProducto={"Ejemplo"} cantidad={2} precio={10} />;
};

export default Productos;
